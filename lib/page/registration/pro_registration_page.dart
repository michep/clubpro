import 'package:flutter/material.dart';

class ProRegistrationPage extends StatelessWidget {
  const ProRegistrationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('профессионал'),
    );
  }
}
